# 合同会社ARCH — エージェント共通ビジネス定義（v5.0）

> [!IMPORTANT]
> ARCH公式HPの改修では、`docs/arch-hp-baseline-spec-2026-09.md`（2026年9月公開版）を最初に全文確認し、最優先の基準仕様として扱うこと。
> `docs/arch-hp-baseline-spec-2026-08-10.md` は旧版（アーカイブ）。矛盾する場合は必ず 2026-09 版を優先する。
> 判断の優先順位は ①ARCHフィロソフィー Ver.2.0 → ②鈴木集が後から確定した最新判断 → ③ARCH AI前提組織 全体設計書 → ④2026年9月公開版のサイト → ⑤旧資料。
> 明確な事業変更または確認済み事実の追加がない限り、大規模な再設計を行わない。

このドキュメントは、合同会社ARCH の各エージェント（Claude Code + Cowork + Design + 外部事務 等）が
共通の語彙でクライアント業務にあたるための「正」の定義です。

最終更新：2026-09-14（**v5.0**：2026年9月に公開したサイトを現行仕様として反映。ブランドディスクリプター「DENTAL OPERATIONS PARTNER」、配色の刷新、CTA「初回適性相談」、ARCHを30秒での4項目差し替え、ブランド画像の扱いを更新。）

---

## 0. ブランド方針（最重要）

ARCH のブランドは **「歯科医院の外部事務長」** をメインに、**「特に訪問歯科運営に強い」** をサブポジションとして打ち出します。
v4.0 までは「訪問歯科の外部事務長」一本軸でしたが、ARCH 本来の支援領域は外来・分院・スタッフ導線・機材導入・ディーラー調整まで医院運営全体に及ぶため、v4.1 で器を広げました。

ターゲットイメージ：
- 院長＋スタッフ10名前後規模
- 訪問歯科を伸ばしたい医院
- 分院展開を考え始めた医院
- 院長が実務を抱え込み始めている医院
- 専属事務長を雇うほどではないが、運営整理が必要な医院

大型法人向けの経営コンサルではなく、**院長の右腕として実務を一緒に整理する外部事務長**。

### 0.1 やるべきこと

- ARCH は **歯科医院の外部事務長** として訴求する
- 訪問歯科は「**特に強い領域**」として位置づけ（訪問歯科専門と見せない）
- 単なるアドバイザーではなく、**現場実務まで入り込む**伴走者として見せる
- 院長一人が抱え始めている実務を一緒に整理する、という距離感を出す
- 代表の現場経験を信用要素として用いる。経歴の正式表記は「元小学校教員 → 医療法人で歯科事務局長を務め、訪問歯科部門を立ち上げ。6か月で黒字化。」とし、**勤務先の所在地は断定しない**（サイト側は `lib/site.ts` の `CAREER_STATEMENT` / `CAREER_STEPS` / `TIMELINE` が正）
- ブランドディスクリプター **DENTAL OPERATIONS PARTNER** を使う。会社名でも商品名でもなく、立ち位置を補足する英語表現。日本語の **歯科医院の外部事務長** を必ず主として併記する。使用場所は TOP の HERO／Footer／会社概要のブランド説明に限定する
- 余白の多い、信頼感のある editorial デザインを保つ
- スマホファースト（移動中・診療合間・夜間に院長が見る前提）
- 自然に追加していい現場ワード：分院立ち上げ / 機材導入 / 現場立会い / ディーラー調整 / オペレーション整理 / 院内導線確認

### 0.2 やめるべきこと

- **AI / エージェント前面表現** — 「13体のエージェント」「ARCHセンサー（独自AI）」「AIエージェント発注システム」等のテック演出は撤廃済み。"AIで作った会社" に見せない。
- **横文字ブランド名** — 「プロデューサー」「ARCH大学 営業学部」「品質診断エンジン」のようなマーケティング造語は不可。普通の事業名を使う。
- **老人ホーム紹介業の記述** — 削除済み（/services/senior-home はディレクトリごと削除済み）。
- **「旧：〇〇」併記** — v4.0 で撤廃。混乱を生む。
- **情報の詰め込みすぎ** — セクションは少なく、文字は短く強く。
- **大型法人向け感／経営戦略会社感／開業コンサル感** — 院長 1〜数名規模の医院に距離が遠く感じられる表現は避ける。
- **「訪問歯科専門」と狭く見せる** — v4.1 で「医院全体を見る」軸に拡張。訪問歯科は強みとして残す。
- **出所のあいまいな写真** — ブランドビジュアルとして承認された画像だけを使う。承認済みの AI 生成画像（`public/images/brand/` の4点）は使用可。ただし**実在の医院・本社・人物の事実写真として誤認させない**（「当社オフィス」「本社」「所在地」等の説明と組み合わせない）。Footer に「※サイト内の一部ビジュアルはブランドイメージです。」を1回だけ表示する。今後は実写が揃えば `lib/brand.ts` の `BRAND_IMAGES` を差し替える

### 0.3 例外として残す内部構造

旧バージョンの「13体エージェント」フレームは公開面（HP・コピー）からは完全撤廃。
ただし、合同会社 ARCH 内部での **業務カテゴリ整理** としては引き続き活用可能（採用・経理・労務・営業アシスタント等の役割棚卸し用途）。**外部に向けては絶対に「13体」を語らない**。

---

## 1. 会社概要

- **社名**：合同会社ARCH（アーチ）
- **代表**：鈴木 集（Atsumu Suzuki）
- **ドメイン**：arch-yh.com
- **中核コンセプト**：**歯科医院の外部事務長**（特に訪問歯科運営に強い）
- **コアコピー**：歯科医院を、"回り続ける"組織へ。
- **ブランドディスクリプター**：DENTAL OPERATIONS PARTNER（日本語は「歯科医院の外部事務長」）
- **経歴（正式表記）**：元小学校教員 → 医療法人で歯科事務局長を務め、訪問歯科部門を立ち上げ。6か月で黒字化。（所在地は断定しない）
- **ビジョン**：現場の『想い』と経営の『現実』をつなぐ架け橋になりたい

---

## 2. サービス構成（v5.0 / 2026年9月公開版）

公開面は **2 本柱**。TOP ではこの二方向だけを示し、細かいサービスを並べない。

| No. | 見せ方（TOP） | サービス名 | URL |
|---|---|---|---|
| 01 | 訪問歯科を立ち上げる・再設計する | 訪問歯科コンサルティング（TOP 表記は「訪問歯科支援」） | `/services/visit-dental-consulting` |
| 02 | 医院経営と運営を支える | 外部事務長 | `/services/external-manager` |

旧 URL（`/services/consulting` `/services/sales` `/bpo-service` `/services/dental-matching` 等）は `next.config.ts` で 301 転送済み。**新しいサービスページを増やさない**。採用・業者連携・DX などは「必要に応じて実行体制も組む」と1行で添えるにとどめる。

### 2.1 支援の二層（ARCHフィロソフィー Ver.2.0）

- **運営レイヤー＝終わらせる仕事**：立ち上げ／書類／記録／請求／採用の型／施設営業の導線／院内運用。医院スタッフだけで回る状態にして完了する
- **経営レイヤー＝価値がある限り続ける仕事**：経営判断／制度改定／人の問題／数字の読み替え／施設との関係／次の展開／承継
- 医院内で担えるようになれば契約終了。これを **卒業** と呼ぶ。契約の継続自体を成功として表現しない

### 2.2 中核メッセージ

- **外部事務長**：助言だけでは医院は回らない。決めるところから実際に手を動かすところまで入る
- **訪問歯科コンサルティング**：患者を紹介する仕事ではない。医院の中に体制をつくり、施設との関係を自院で積み上げる

### 2.3 料金（**要最終判断**。エージェントが勝手に変えない）

現在のサイト表記（2026-08-22 改定のまま、2026年9月改修でも変更していない）：

- 外部事務長｜運営伴走 ¥150,000/月（税別）
- 外部事務長｜経営伴走 ¥300,000/月（税別）
- いずれも最低契約期間6か月。初期費用は支援範囲に応じて別途見積もり
- 訪問歯科コンサルティングは期間・費用とも個別見積もり
- TOP に料金は載せない。5万円の相談顧問プランは掲載しない

ARCHフィロソフィー Ver.2.0 の商品定義（標準立ち上げ支援 30万円×6か月／自走型立ち上げ支援 15万円×6か月／継続外部事務長 原則15万円）とは意味が一致していない。**この不一致は鈴木集の最終判断事項**であり、判断が出るまでサイト表記・構造化データ・metadata の金額を変更しない。詳細は `docs/CONTENT_DECISIONS_REQUIRED.md`。

### 2.4 初回適性相談（無料）

- 医院の目的・現状・体制・資金・訪問歯科との適性・ARCHとの相性を確認する場
- 詳細な診断書／実行計画書／資料の作成／同行／実装の責任は**含めない**（責任は有料）
- 「何でも無料相談」に見せない

## 3. ARCH ノート（コラム）

**呼称**：`ARCH NOTE — 訪問歯科の現場メモ`
**URL**：`/columns`（インデックスページあり）
**個別記事 URL**：`/columns/{slug}`
**全 13 本**（既存記事はすべて温存。本文は触らない）

### 3.1 トーン

- SEO 構造化記事の見え方は弱める
- 「現場メモ」「外部事務長の手元から」という空気感
- 過剰な装飾なし、editorial v2 のフラットボーダー基調

### 3.2 旧 SEO 記事 3 本（別系統）

`/column/houmon-shika-hajimekata`, `/column/houmon-shika-santei`, `/column/houmon-shika-eigyo` の 3 本は旧 SEO 系列で、`app/column/`（単数形）配下に独自レイアウトで存在。**触らない**（過去の流入経路として残す）。

---

## 4. エージェント共通ルール

1. **サービス名は v5.0 表記（外部事務長／訪問歯科コンサルティング）を使用する**。旧名（プロデューサー / 営業学部 / 暮らしの選択相談所 / 品質診断エンジン / 外付け事務局 / 事務代行・BPO支援 / 歯科医院運営支援）は今後一切使用しない。**「（旧：〇〇）」併記もしない**。
2. **コピー／原稿執筆時** は、各サービスの「中核メッセージ」（2.2節）に沿うこと。
3. **AI / エージェント前面表現を作らない**。「AI が自動診断」「13体のエージェントが連動」「最強の外部エンジン」等は禁句。
4. **横文字ブランド造語を作らない**。普通の事業名で書く。
5. **簡易診断ツールは公開面から撤去済み**（`/services/dental-matching` は訪問歯科ページへ転送）。新たに導線を復活させない。
6. **CTA は「初回適性相談」に統一**（`components/ui/Cta.tsx` の `CONSULT_LABEL` が正。旧「無料相談を予約する」「相談する」「初回相談」は使わない）。
7. **問い合わせ先**：`/#contact`（トップ Contact フォーム / FormSubmit.co 経由）。
8. **スマホファースト**：移動中・診療合間・夜間にスマホで見られる前提。文字は短く、ボタンは押しやすく、結論は先に。

---

## 5. News 追加ワークフロー

ユーザー（鈴木集）から「News に○○を追加して」「お知らせに○○を入れて」等の依頼があった場合、エージェントは下記の手順を**必ず厳守**すること。探索や推測をせず、このワークフロー通りに実行する。

### 5.1 データソースの場所

News データは `lib/news.ts` の `NEWS` 配列に一元管理されている。データ構造は以下：

```ts
// lib/news.ts
export type NewsCategory = "お知らせ" | "更新" | "リリース" | "コラム" | "プレス";

export type NewsItem = {
  date: string;        // YYYY-MM-DD 形式（必須）
  category: NewsCategory; // 5種類から選択（必須）
  title: string;       // 「〇〇を更新しました」等（必須）
  href: string;        // 内部リンク "/services/xxx" or 外部URL "https://..."（必須）
  external?: boolean;  // 省略可。https:// で始まる場合は自動判定されるため通常不要
};

export const NEWS: NewsItem[] = [
  // 先頭が最新。新しい記事はここに追記する
  { date: "2026-04-10", category: "リリース", title: "...", href: "..." },
  // ...
];
```

### 5.2 追加手順（5ステップ）

1. **`lib/news.ts` を Read** し、現在の配列構造とインデントを確認する
2. **配列の先頭（`NEWS: NewsItem[] = [` の直後）に1オブジェクト追記** する。既存エントリより上に置くこと（新しい順を維持）
3. **TypeScript 型チェック** を実施（`./node_modules/.bin/tsc --noEmit -p tsconfig.json`）。`.next/types/validator.ts` の既存警告は無視して可
4. **`git add lib/news.ts && git commit`** でコミット作成。コミットメッセージは `news: <タイトル要約>` の形式
5. **ユーザーに `git push origin main` の実行を依頼** する（VM からは HTTPプロキシ制限で push 不可）

### 5.3 必須フィールドの決定ルール

- **date**：ユーザーが明示しなければ「今日」を使う。`date` コマンドで実際の日付を取得し、YYYY-MM-DD 形式で記載すること
- **category**：ユーザーが明示しなければ文脈から自動判定
  - 新サービス・新機能・新ページ公開 → `"リリース"`
  - 既存ページ・コンテンツの更新 → `"更新"`
  - 休業・営業時間変更・その他通知 → `"お知らせ"`
  - 新コラム記事公開 → `"コラム"`
  - メディア掲載・プレスリリース → `"プレス"`
- **title**：ユーザーが言った文言を尊重しつつ、句読点や体裁を整える。末尾は体言止めor「〜しました」で統一
- **href**：内部リンクの場合は `/` 始まりの絶対パス。外部リンクは `https://` 始まりのフル URL（`external: true` の明示は不要、自動判定される）

### 5.4 禁止事項

- ❌ 配列の**末尾**に追記すること（古い順になってしまう）
- ❌ `date` を適当に決めること（必ず `date` コマンドで今日の日付を取得するか、ユーザー指定に従う）
- ❌ `git push` を VM から試みること（403 で失敗する。必ずユーザーに手動プッシュを依頼）
- ❌ `lib/news.ts` 以外のファイル（`app/news/page.tsx`, `app/page.tsx`）を編集すること。**これらは `lib/news.ts` を import しているだけなので、データを変えれば自動で反映される**
- ❌ カテゴリを勝手に新設すること（`NewsCategory` 型の5種類から選ぶ。新カテゴリが必要な場合はユーザーに確認）
- ❌ **旧サービス名（プロデューサー、営業学部、品質診断エンジン、外付け事務局など）を news タイトルに使うこと**

### 5.5 依頼例とエージェントの反応

**例1：** 「News に『歯科医院運営支援』のページを更新したって入れて」
→ date: 今日 / category: "更新" / title: 「歯科医院運営支援ページを更新しました」 / href: "/services/consulting"

**例2：** 「日経メディカルに記事が載ったから News に追加。URL は https://...」
→ date: 今日（またはユーザー指定日） / category: "プレス" / title: 「日経メディカルに掲載されました」 / href: URL

**例3：** 「5月1日からGW休業のお知らせを News に」
→ date: 今日 / category: "お知らせ" / title: 「ゴールデンウィーク休業のお知らせ（5/1〜5/6）」 / href: "/news"

---

## 6. 技術アーキテクチャ・サイト構成

### 6.1 技術スタック

- **フレームワーク**：Next.js 16.1.6（App Router）
- **言語**：TypeScript
- **React**：19.2.3
- **スタイリング**：Tailwind CSS v4 + `app/globals.css` のデザイントークン（`arch-*`）
- **書体**：Noto Sans JP / Noto Serif JP / Inter（`next/font/google`）
- **アイコン**：lucide-react
- **ホスティング**：Vercel（GitHub `main` ブランチへの push で自動デプロイ）
- **ドメイン**：arch-yh.com
- **リポジトリ**：github.com/atsumusuzuki-art/arch-corporate-site
- **ローカル作業ディレクトリ**：`/Volumes/ARCH_SSD/arch-corporate-site`（鈴木氏の Mac 上の外部 SSD）

### 6.2 デザイン規約（2026年9月版）

- **カラー**：Paper `#F7F4EE` / White `#FFFFFF` / ARCH Green `#0F5C3E` / Deep Green `#08533C` / Forest `#2C4A3B` / Pale Green `#E6F2ED` / Light Green `#A7D6B0` / Ink `#1A1A1A` / Muted `#6B716D` / Line `#D5D2CB`
- **比率の目安**：Paper・White 約7割 ／ Deep Green・Green 約2割 ／ Pale Green 等 約1割
- **Gold は標準色として使用しない**（トークン名 `arch-gold*` は互換のため残っているが、値は緑系に置き換え済み）
- トークンは `app/globals.css` に集約。`arch-paper` / `arch-white` / `arch-green` / `arch-deep` / `arch-pale` / `arch-light` / `arch-line` / `arch-muted` と、旧名の互換エイリアス
- **書体**：Noto Sans JP（本文・ナビ・ボタン・基本の見出し）／ Noto Serif JP（`serif-jp`。HERO・ARCH Philosophy・代表のことばなど用途限定）／ Inter（`mono-micro` `mono-label` `num-en`。英字ラベル・番号・メタ情報）
- **NG**：Glassmorphism・半透明カード・強い blur・3D・光彩・青紫や紫系・派手なグラデーション・大きな角丸・意味のないアイコン・過剰なアニメーション・スクロールジャック・動画背景・歯や握手のアイコン・新しいARCHマーク
- **OK**：1px 罫線／番号＋英字ラベル／大きな余白／角丸 0〜4px／影なし／静かな動き（240ms のフェード＋8px、罫線の line reveal、`prefers-reduced-motion` 対応）
- **レイアウト**：最大幅 1280px、本文幅 600〜660px、セクション余白は PC 112〜136px・スマホ 76px（`lib/ui.ts` の `CONTAINER` `SECTION_Y` `PROSE_W` `H2` `BODY`）
- **ロゴ**：現行ロゴのみ。表示は `components/brand/BrandLogo.tsx` 経由に限定し、参照先は `lib/brand.ts` の `BRAND_LOGO`。新ロゴ案（OPEN ARCH／新Aマーク／新WORDMARK）は使わない。3期目以降の切り替えは `BRAND_LOGO` の差し替えだけで済む構造
- **ブランド画像**：`public/images/brand/` の4点（hero-arch-glass／director-meeting／implementation-plan／arch-philosophy-books）。「相談 → 判断 → 実装 → 仕組み化」の順で使う。パス・alt・表示位置は `lib/brand.ts` の `BRAND_IMAGES` / `HERO_BACKGROUND` に集約
- **OGP**：`/images/og-arch-2026.jpg`（1200×630。HERO と同じガラスサインの切り出し）。参照は `lib/site.ts` の `OG_IMAGE`

### 6.3 ファイル構成（v5.0）

```
app/
├── layout.tsx                      # metadata・JSON-LD・フォント（Noto Sans JP / Noto Serif JP / Inter）
├── page.tsx                        # TOP（Server Component）
├── globals.css                     # デザイントークン・書体クラス・HERO のグラデーション
├── services/external-manager/      # 外部事務長
├── services/visit-dental-consulting/ # 訪問歯科コンサルティング
├── cases/ , cases/[slug]/          # 支援実績（データは lib/cases.ts）
├── company/ , privacy/ , thanks/
├── columns/ , column/              # コラム（column/ は旧SEO記事。触らない）
└── 旧ページ群                        # 転送済み。原則触らない
components/
├── SiteHeader.tsx / SiteFooter.tsx / PageHero.tsx / ContactForm.tsx / Reveal.tsx など
├── brand/  BrandLogo・BrandWordmark・BrandSignature
└── ui/     BrandPhoto・SectionHeader・Cta・CaseRow・TwoLayers・PhilosophyBlock・StepList・RelatedLinks
lib/
├── site.ts   # ナビ・HEROコピー・30秒・二層・代表の役割・経歴・支援地域・PAGE_DATES・OG_IMAGE
├── brand.ts  # ロゴ・ディスクリプター・ブランド画像4点・HERO背景
├── ui.ts     # 共通レイアウトクラス
├── cases.ts  # 支援実績データ（数字・時点・現在地）
└── columns/  # コラム本文
public/images/brand/                # ブランド画像4点
```

### 6.4 トップページ（`app/page.tsx`）のセクション構成（v5.0）

```
Header → ① HERO → ② ARCHを30秒で → ③ 院長と、同じ目線で考える → ④ 代表・鈴木集
→ ⑤ 支援実績 → ⑥ 現場で実装する → ⑦ 支援の二層 → ⑧ サービス
→ ⑨ ARCH Philosophy → ⑩ 支援地域 → ⑪ コラム → ⑫ 初回適性相談 → Footer
```

- **ナビ**：外部事務長 / 訪問歯科 / 支援実績 / コラム / 会社概要 ＋ CTA「初回適性相談」
- **HERO**：`hero-arch-glass.png` を全面の背景にし、Paper 色の半透明グラデーションを重ねる。見出しは「歯科医院を、"回り続ける"組織へ。」。画像に文字やロゴを重ねない
- **ARCHを30秒で**（4項目。フィロソフィーの仕事の進め方）：
  1. 良いご縁から始める
  2. 院長と現場を見る
  3. 現場で実装する
  4. 仕組みとして残す
- **支援実績**：CASE ROW（CASE番号／地域／確認できた数字／課題→ARCHの関与→確認できた結果→現在地）。数字と時点は変更禁止。「現在地」には時間で古くなる表現を書かない
- **ARCH Philosophy**：良いご縁から始める。／実装で価値を生む。／仕組みとして現場に残す。＋ 医院は自走させる。／それでも選ばれる仕事をする。
- **相談**：`components/ContactForm.tsx`（FormSubmit.co 経由 → `/thanks`）

### 6.5 旧ページの扱い

- `/services/consulting` `/services/sales` `/services/dental-matching` `/bpo-service` `/news` などの旧 URL は `next.config.ts` で 301 転送済み。ファイルは残っているが**公開面からは到達しない**ため、原則として編集しない（旧配色・旧コピーが残っているのはこのため）
- 簡易診断ツール（`dental-matching/DentalMatchingClient.tsx`）も転送済み。導線を復活させない
- `app/services/visit-dental-consulting/lp-sample/` は未コミットの作業中ファイル。触らない

### 6.6 Git ワークフロー・制約

- **VM（Cowork）からの `git push` は不可**（HTTP プロキシ 403）。VM 側で `git add` + `git commit` まで行い、ユーザー（鈴木氏）に `git push origin main` を依頼する
- ユーザーの push 手順：`cd /Volumes/ARCH_SSD/arch-corporate-site && git push origin main`
- push 後は Vercel が自動デプロイ（通常2〜3分）
- `npm run build`（`next build`）は VM 内では SWC バイナリ欠落により実行不可。`./node_modules/.bin/tsc --noEmit -p tsconfig.json` で型チェックのみ実施。`.next/types/validator.ts` の既存警告（`/columns` vs `/column` ルート不一致）は無視して可
- `.git/index.lock` が macOS Finder 由来で残ることがあるため、git 操作前に `rm -f .git/index.lock` を入れると安全

### 6.7 コラム（ARCH NOTE）記事の追加パターン

1. `app/columns/<slug>/page.tsx` を作成。既存記事（例：`broker-trap`）をテンプレートとして使用
2. `export const metadata: Metadata` で title / description を定義
3. 本文は `<article className="py-16 md:py-24">` → `<div className="max-w-3xl mx-auto px-4 sm:px-6">` → `<div className="prose-custom">` の入れ子構造
4. `app/columns/page.tsx` の `NOTES` 配列の先頭に 1 エントリ追加（slug / category / title / date）
5. `app/columns/layout.tsx` の `ALL_NOTES` 配列にも 1 エントリ追加（関連メモ表示用）
6. 必要に応じ `app/page.tsx` の `NOTES` 配列を更新（トップに掲載する厳選 3 本を入れ替える場合）
7. `lib/news.ts` の `NEWS` 配列先頭にコラム公開エントリを追加（News 追加ワークフロー＝第 5 章に従う）
8. `app/sitemap.ts` に URL 追加

### 6.8 最近のコミット履歴（参考、v5.0 関連）

```
7491aed feat: サイト全面改修（DENTAL OPERATIONS PARTNER／HERO刷新）
d7b914e chore: 代表写真を差し替える
9beb9d2 chore: サイト上のメールアドレス表示を問い合わせフォームへの導線に置き換える
```

---

## 7. 変更履歴

- **v5.0 (2026-09-14)**：2026年9月公開版を現行仕様として反映。ブランドディスクリプター「DENTAL OPERATIONS PARTNER」＋日本語「歯科医院の外部事務長」を正式表現に。配色を Paper／White／Deep Green 中心へ変更し Gold を標準色から外す。CTA を「初回適性相談」に統一。ARCHを30秒での4項目をフィロソフィー準拠に差し替え。承認済みブランド画像4点の使用ルールを追加（実在施設と誤認させない・Footer に注記1回）。OGP を現行ブランドへ差し替え。代表経歴の所在地表記を統一。サービス構成を2本柱に更新。料金は未決のため据え置き。
- **v4.1 (2026-05-12)**：中核コンセプトを「歯科医院の外部事務長（特に訪問歯科運営に強い）」へ拡張。HERO コピーを「歯科医院を、回り続ける組織へ。」に変更、サブコピーを医院運営全体（外来・訪問歯科・分院展開・スタッフ導線・レセプト・現場オペレーション）に拡張。サービス 01 を「訪問歯科運営支援」→「歯科医院運営支援」にリネーム。CASE 04「分院立ち上げ支援」を新規追加。STRENGTHS の最初を「医院運営に密着」に変更、訪問歯科は別 strength として残す。SEO メタも歯科医院系を主に、訪問歯科を強み枠で配置。
- **v4.0 (2026-05-11)**：**「訪問歯科の外部事務長」一本に集約。** 旧 5 ブランド名（プロデューサー / 営業学部 / 暮らしの選択相談所 / 品質診断エンジン / 外付け事務局）を撤廃し、3 メインページ + 1 補助ツール構成へ。13 体エージェント・AI 前面表現・横文字ブランドの全削除。老人ホーム紹介事業を削除。コラムは ARCH NOTE として整理。ブランド方針節（0 章）を新設。
- **v3.0 (2026-04-20)**：技術アーキテクチャ・サイト構成節（第6章）を追記。
- **v2.1 (2026-04-11)**：News 追加ワークフロー節（第5章）を追記。
- **v2.0 (2026-04-10)**：サービス名全面刷新。5 事業体制に移行（v4.0 で撤廃）。
- **v1.0**：初版リリース。訪問歯科コンサル／営業コンサル／老人ホーム紹介の 3 事業体制。
