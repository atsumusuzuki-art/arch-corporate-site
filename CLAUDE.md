# 合同会社ARCH — エージェント共通ビジネス定義（v4.1）

このドキュメントは、合同会社ARCH の各エージェント（Claude Code + Cowork + Design + 外部事務 等）が
共通の語彙でクライアント業務にあたるための「正」の定義です。

最終更新：2026-05-12（**v4.1**：中核コンセプトを「歯科医院の外部事務長（特に訪問歯科運営に強い）」へ拡張。v4.0 の「訪問歯科の外部事務長」一本軸を医院運営全体に広げ、訪問歯科は「特に強い領域」として残す。）

---

## 0. ブランド方針（最重要）

ARCH のブランドは **「歯科医院の外部事務長」** をメインに、**「特に訪問歯科運営に強い」** をサブポジションとして打ち出します。
v4.0 までは「訪問歯科の外部事務長」一本軸でしたが、ARCH 本来の支援領域は外来・分院・スタッフ導線・機材導入・ディーラー調整まで医院運営全体に及ぶため、v4.1 で器を広げました。

ターゲットイメージ：
- 院長＋スタッフ10名前後規模
- 訪問歯科を伸ばしたい医院
- 分院展開を考え始めた医院
- 院長が実務を抱え込み始めている医院
- 専属事務長を雇うほどではないが、運営整理が必要な医院

大型法人向けの経営コンサルではなく、**院長の右腕として実務を一緒に整理する外部事務長**。

### 0.1 やるべきこと

- ARCH は **歯科医院の外部事務長** として訴求する
- 訪問歯科は「**特に強い領域**」として位置づけ（訪問歯科専門と見せない）
- 単なるアドバイザーではなく、**現場実務まで入り込む**伴走者として見せる
- 院長一人が抱え始めている実務を一緒に整理する、という距離感を出す
- 代表の現場経験（元小学校教員 → 歯科事務局長 → 訪問歯科立ち上げ・6ヶ月黒字化）を信用要素として用いる
- 余白の多い、信頼感のある editorial デザインを保つ
- スマホファースト（移動中・診療合間・夜間に院長が見る前提）
- 自然に追加していい現場ワード：分院立ち上げ / 機材導入 / 現場立会い / ディーラー調整 / オペレーション整理 / 院内導線確認

### 0.2 やめるべきこと

- **AI / エージェント前面表現** — 「13体のエージェント」「ARCHセンサー（独自AI）」「AIエージェント発注システム」等のテック演出は撤廃済み。"AIで作った会社" に見せない。
- **横文字ブランド名** — 「プロデューサー」「ARCH大学 営業学部」「品質診断エンジン」のようなマーケティング造語は不可。普通の事業名を使う。
- **老人ホーム紹介業の記述** — 削除済み（/services/senior-home はディレクトリごと削除済み）。
- **「旧：〇〇」併記** — v4.0 で撤廃。混乱を生む。
- **情報の詰め込みすぎ** — セクションは少なく、文字は短く強く。
- **大型法人向け感／経営戦略会社感／開業コンサル感** — 院長 1〜数名規模の医院に距離が遠く感じられる表現は避ける。
- **「訪問歯科専門」と狭く見せる** — v4.1 で「医院全体を見る」軸に拡張。訪問歯科は強みとして残す。
- **AI生成写真** — 過去経緯で排除済み。実写以外は抽象表現で。

### 0.3 例外として残す内部構造

旧バージョンの「13体エージェント」フレームは公開面（HP・コピー）からは完全撤廃。
ただし、合同会社 ARCH 内部での **業務カテゴリ整理** としては引き続き活用可能（採用・経理・労務・営業アシスタント等の役割棚卸し用途）。**外部に向けては絶対に「13体」を語らない**。

---

## 1. 会社概要

- **社名**：合同会社ARCH（アーチ）
- **代表**：鈴木 集（Atsumu Suzuki）
- **ドメイン**：arch-yh.com
- **中核コンセプト**：**歯科医院の外部事務長**（特に訪問歯科運営に強い）
- **コアコピー**：歯科医院を、"回り続ける組織" へ。
- **経歴**：元小学校教員 → 横浜市の広域医療法人 歯科事務局長（経営再建を主導） → 北海道の医療グループにて訪問歯科を単独立ち上げ・6ヶ月で黒字化
- **ビジョン**：現場の『想い』と経営の『現実』をつなぐ架け橋になりたい

---

## 2. サービス構成（v4.0）

公開面では **3 メインページ + 1 補助ツール** 構成。トップでは便宜上 5 サービスとして並列表示するが、URL ベースでは 3 ページに集約されている。

### 2.1 トップ表記の 5 サービス（順序固定）

| No. | サービス名 | URL | 役割 |
|---|---|---|---|
| 01 | **歯科医院運営支援** | `/services/consulting` | 月次分析・運営改善・導線整理・施設対応・経営支援（メインページ） |
| 02 | **レセプト・算定改善支援** | `/services/consulting#billing` | 算定漏れ確認・カルテチェック・返戻対策・精度改善（運営コンサル内 02 機能） |
| 03 | **施設連携・営業支援** | `/services/sales` | 施設対応・営業導線・初診導線・トラブル防止（メインページ） |
| 04 | **事務代行・BPO支援** | `/bpo-service` | 書類作成・郵送業務・業務整理・事務フロー改善（メインページ） |
| 05 | **DX・業務改善支援** | `/bpo-service#dx` | 訪問歯科記録アプリ・シフト管理・業務デジタル化・実務効率改善（BPO 内 03 機能） |

### 2.2 補助ツール（独立 URL で残るが、独立事業として見せない）

| 名称 | URL | 位置づけ |
|---|---|---|
| 施設の歯科対応 簡易診断ツール | `/services/dental-matching` | 「施設連携・営業支援」内の補助ツール（無料）。`/services/sales` の 04 セクションからリンク。**「品質診断エンジン」というブランド名は使用しない**。 |

### 2.3 各サービスの中核メッセージ

#### 01. 歯科医院運営支援（`/services/consulting`）
- **中核メッセージ**：助言だけでは、医院は回らない。外部事務長として実務に入り込む。
- HERO コピー：「歯科医院を、回し続ける。」
- カバー領域：外来・訪問歯科・分院展開・スタッフ導線・現場改善・月次分析・レセプト/算定・オペレーション整理
- 3 機能：① 運営改善・導線整理 / ② レセプト・算定改善 / ③ 経営判断の伴走
- 特に強い領域：訪問歯科運営（立ち上げ・黒字化の現場経験）、分院立ち上げの現場立会い、ディーラー調整
- **料金（v4.1 で関与度別 3 プランに改定）**：
  - 顧問プラン ¥50,000〜/月（税別）：チャット相談・月1回オンライン
  - 外部事務長ライト ¥100,000〜/月（税別、おすすめ）：月1回訪問 + 定例会 + AI活用簡易制作
  - 外部事務長 ¥300,000〜/月（税別、上位プラン）：週1回ペース伴走、経営会議参加、訪問立ち上げ・分院展開・採用・施設連携・補助金まで横断支援
  - 旧 Light/Standard/Premium 3プラン + 初回お試し 7万 + 単発実地 5万は撤廃済み

#### 02. レセプト・算定改善支援（`/services/consulting#billing`）
- 歯科医院運営支援内の 02 機能として内包
- 算定漏れ確認・カルテチェック・返戻対策・訪問歯科特有の算定ナレッジ蓄積
- 単独ページは持たない（`/services/consulting` の 02 セクションへアンカー遷移）

#### 03. 施設連携・営業支援（`/services/sales`）
- **中核メッセージ**：ブローカー依存から、自院で積み上げる関係資産へ。
- HERO コピー：「施設との信頼を、院内の仕組みに。」
- 3 機能：① 施設対応導線 / ② 初診オペレーション / ③ 営業の仕組み化
- 04 セクションに「施設の歯科対応 簡易診断ツール」（=旧品質診断エンジン）への導線あり

#### 04. 事務代行・BPO支援（`/bpo-service`）
- **中核メッセージ**：必要な業務を、必要な分だけ、スポットで。
- HERO コピー：「事務作業を、医院の外側に。」
- 3 機能：① 書類業務代行 / ② 郵送・業務フロー / ③ DX・業務改善
- **料金**：施設・ご家族向け資料作成 ¥5万〜/件 / **ホームページ制作・改修 ¥15万〜/一式（税別）BEST VALUE** / カルテ・レセプトBPO 月額要相談
- **「13体のエージェント」「最強の外部エンジン」「CORE 3体」「AIエージェント発注システム」表現は v4.0 で完全撤廃済み**

#### 05. DX・業務改善支援（`/bpo-service#dx`）
- 事務代行・BPO支援 内の 03 機能として内包
- 訪問歯科記録アプリ・シフト管理・業務デジタル化
- 単独ページは持たない（`/bpo-service` の 03 セクションへアンカー遷移）

#### 補助ツール：施設の歯科対応 簡易診断（`/services/dental-matching`）
- **位置づけ**：施設連携・営業支援の補助ツール（独立事業として見せない）
- 6 項目 × 5 段階 Likert：対応スピード／報告書の質／接遇マナー／現場連携／コンプライアンス／継続性
- **判定ロジック**（閾値は固定。**v4.0 でも保持**）：
  - LOW（平均 < 2.5）：即刻変えるべき → 切り替え支援パッケージを提示
  - MID（2.5 ≤ 平均 < 4.0）：現在の歯科医院と相談 → 低スコア項目を相談項目化
  - HIGH（平均 ≥ 4.0）：素晴らしい歯科医院 → 長期継続のコツを提示
- 共通CTA：「詳細な改善プランを鈴木に相談する」
- **「ARCHセンサー・ロジック」「品質診断エンジン」呼称は v4.0 で撤廃**。コード上もブランド呼称を消し、シンプルに「診断ツール／判定の考え方」と表記。

---

## 3. ARCH ノート（コラム）

**呼称**：`ARCH NOTE — 訪問歯科の現場メモ`
**URL**：`/columns`（インデックスページあり）
**個別記事 URL**：`/columns/{slug}`
**全 13 本**（既存記事はすべて温存。本文は触らない）

### 3.1 トーン

- SEO 構造化記事の見え方は弱める
- 「現場メモ」「外部事務長の手元から」という空気感
- 過剰な装飾なし、editorial v2 のフラットボーダー基調

### 3.2 旧 SEO 記事 3 本（別系統）

`/column/houmon-shika-hajimekata`, `/column/houmon-shika-santei`, `/column/houmon-shika-eigyo` の 3 本は旧 SEO 系列で、`app/column/`（単数形）配下に独自レイアウトで存在。**触らない**（過去の流入経路として残す）。

---

## 4. エージェント共通ルール

1. **サービス名は v4.0 表記を使用する**。旧名（プロデューサー / 営業学部 / 暮らしの選択相談所 / 品質診断エンジン / 外付け事務局）は今後一切使用しない。**「（旧：〇〇）」併記もしない**。
2. **コピー／原稿執筆時** は、各サービスの「中核メッセージ」（2.3節）に沿うこと。
3. **AI / エージェント前面表現を作らない**。「AI が自動診断」「13体のエージェントが連動」「最強の外部エンジン」等は禁句。
4. **横文字ブランド造語を作らない**。普通の事業名で書く。
5. **判定ロジック**（簡易診断ツール）は LOW < 2.5 / MID 2.5–3.9 / HIGH ≥ 4.0 の閾値を維持。
6. **CTA は統一**：「無料相談を予約する」または「無料オンライン相談を予約する」、診断結果からの導線は「詳細な改善プランを鈴木に相談する」。
7. **問い合わせ先**：`/#contact`（トップ Contact フォーム / FormSubmit.co 経由）。
8. **スマホファースト**：移動中・診療合間・夜間にスマホで見られる前提。文字は短く、ボタンは押しやすく、結論は先に。

---

## 5. News 追加ワークフロー

ユーザー（鈴木集）から「News に○○を追加して」「お知らせに○○を入れて」等の依頼があった場合、エージェントは下記の手順を**必ず厳守**すること。探索や推測をせず、このワークフロー通りに実行する。

### 5.1 データソースの場所

News データは `lib/news.ts` の `NEWS` 配列に一元管理されている。データ構造は以下：

```ts
// lib/news.ts
export type NewsCategory = "お知らせ" | "更新" | "リリース" | "コラム" | "プレス";

export type NewsItem = {
  date: string;        // YYYY-MM-DD 形式（必須）
  category: NewsCategory; // 5種類から選択（必須）
  title: string;       // 「〇〇を更新しました」等（必須）
  href: string;        // 内部リンク "/services/xxx" or 外部URL "https://..."（必須）
  external?: boolean;  // 省略可。https:// で始まる場合は自動判定されるため通常不要
};

export const NEWS: NewsItem[] = [
  // 先頭が最新。新しい記事はここに追記する
  { date: "2026-04-10", category: "リリース", title: "...", href: "..." },
  // ...
];
```

### 5.2 追加手順（5ステップ）

1. **`lib/news.ts` を Read** し、現在の配列構造とインデントを確認する
2. **配列の先頭（`NEWS: NewsItem[] = [` の直後）に1オブジェクト追記** する。既存エントリより上に置くこと（新しい順を維持）
3. **TypeScript 型チェック** を実施（`./node_modules/.bin/tsc --noEmit -p tsconfig.json`）。`.next/types/validator.ts` の既存警告は無視して可
4. **`git add lib/news.ts && git commit`** でコミット作成。コミットメッセージは `news: <タイトル要約>` の形式
5. **ユーザーに `git push origin main` の実行を依頼** する（VM からは HTTPプロキシ制限で push 不可）

### 5.3 必須フィールドの決定ルール

- **date**：ユーザーが明示しなければ「今日」を使う。`date` コマンドで実際の日付を取得し、YYYY-MM-DD 形式で記載すること
- **category**：ユーザーが明示しなければ文脈から自動判定
  - 新サービス・新機能・新ページ公開 → `"リリース"`
  - 既存ページ・コンテンツの更新 → `"更新"`
  - 休業・営業時間変更・その他通知 → `"お知らせ"`
  - 新コラム記事公開 → `"コラム"`
  - メディア掲載・プレスリリース → `"プレス"`
- **title**：ユーザーが言った文言を尊重しつつ、句読点や体裁を整える。末尾は体言止めor「〜しました」で統一
- **href**：内部リンクの場合は `/` 始まりの絶対パス。外部リンクは `https://` 始まりのフル URL（`external: true` の明示は不要、自動判定される）

### 5.4 禁止事項

- ❌ 配列の**末尾**に追記すること（古い順になってしまう）
- ❌ `date` を適当に決めること（必ず `date` コマンドで今日の日付を取得するか、ユーザー指定に従う）
- ❌ `git push` を VM から試みること（403 で失敗する。必ずユーザーに手動プッシュを依頼）
- ❌ `lib/news.ts` 以外のファイル（`app/news/page.tsx`, `app/page.tsx`）を編集すること。**これらは `lib/news.ts` を import しているだけなので、データを変えれば自動で反映される**
- ❌ カテゴリを勝手に新設すること（`NewsCategory` 型の5種類から選ぶ。新カテゴリが必要な場合はユーザーに確認）
- ❌ **旧サービス名（プロデューサー、営業学部、品質診断エンジン、外付け事務局など）を news タイトルに使うこと**

### 5.5 依頼例とエージェントの反応

**例1：** 「News に『歯科医院運営支援』のページを更新したって入れて」
→ date: 今日 / category: "更新" / title: 「歯科医院運営支援ページを更新しました」 / href: "/services/consulting"

**例2：** 「日経メディカルに記事が載ったから News に追加。URL は https://...」
→ date: 今日（またはユーザー指定日） / category: "プレス" / title: 「日経メディカルに掲載されました」 / href: URL

**例3：** 「5月1日からGW休業のお知らせを News に」
→ date: 今日 / category: "お知らせ" / title: 「ゴールデンウィーク休業のお知らせ（5/1〜5/6）」 / href: "/news"

---

## 6. 技術アーキテクチャ・サイト構成

### 6.1 技術スタック

- **フレームワーク**：Next.js 16.1.6（App Router）
- **言語**：TypeScript
- **React**：19.2.3
- **スタイリング**：Tailwind CSS + 独自 `arch-*` カラーパレット
- **アイコン**：lucide-react
- **ホスティング**：Vercel（GitHub `main` ブランチへの push で自動デプロイ）
- **ドメイン**：arch-yh.com
- **リポジトリ**：github.com/atsumusuzuki-art/arch-corporate-site
- **ローカル作業ディレクトリ**：`/Volumes/ARCH_SSD/arch-corporate-site`（鈴木氏の Mac 上の外部 SSD）

### 6.2 デザイン規約（editorial v2）

- **カラーパレット**：`arch-forest` / `arch-cream` / `arch-cream-raised` / `arch-ink` / `arch-moss` / `arch-gold` / `arch-sage` / `arch-rule` / `arch-rule-dark` / `arch-forest-soft` / `arch-danger`
- **基調**：深緑（arch-forest）+ クリーム（arch-cream）+ ゴールド（arch-gold）+ 薄グレー（arch-rule / arch-sage）
- **NG 表現**：`rounded-2xl` / box-shadow（深いもの）/ gradient 多用 / 黒ベース重厚すぎる装飾 / アニメーション多用
- **OK 表現**：フラットボーダー（border + arch-rule）/ 細い罫線で区切る editorial レイアウト / 余白多め / 文字は短く強く
- **カスタムクラス**：`display-jp`（和文ディスプレイ書体）/ `mono-label`（英大文字ラベル）/ `mono-micro`（極小英ラベル）/ `prose-custom`（コラム本文用）
- **コーナーマーカー**：`@/components/CornerMarkers` から `CornerMarkers` と `SectionTag` を import して使用
- **共通フォント**：Geist + Geist Mono + Noto Sans JP（`app/layout.tsx` で読み込み）

### 6.3 ファイル構成（v4.0）

```
app/
├── layout.tsx              # ルートレイアウト（metadata、JSON-LD、フォント）
├── page.tsx                # トップページ（"use client" — Nav/Hero/About/Cases/Service/Note/Message/Contact/Footer）
├── globals.css             # Tailwind + arch-* tokens + prose-custom
├── sitemap.ts              # SEO サイトマップ
├── robots.ts
├── news/page.tsx           # /news — お知らせ一覧（Server Component）
├── bpo-service/
│   ├── layout.tsx          # 事務代行・BPO支援 専用レイアウト
│   └── page.tsx            # 事務代行・BPO支援
├── services/
│   ├── layout.tsx          # サービスページ共通レイアウト
│   ├── consulting/page.tsx # 歯科医院運営支援（#billing アンカーあり）
│   ├── sales/page.tsx      # 施設連携・営業支援（dental-matching 案内を内包）
│   └── dental-matching/
│       ├── page.tsx                  # Server wrapper（metadata のみ）
│       └── DentalMatchingClient.tsx  # 補助診断ツール UI（閾値ロジックを保持）
├── columns/
│   ├── layout.tsx          # ARCH NOTE 共通レイアウト
│   ├── page.tsx            # /columns — ARCH NOTE インデックス
│   └── {slug}/             # 全 13 本のメモ（既存本文は v4.0 でも温存）
└── column/                 # 旧 SEO 記事 3 本（触らない）
    ├── layout.tsx
    └── houmon-shika-*/
components/
├── CornerMarkers.tsx       # SectionTag / CornerMarkers
└── ServiceInquiryForm.tsx  # 申込フォーム（お試し / 単発実地支援）
lib/
└── news.ts                 # News データソース
public/images/
├── logo.jpg                # 会社ロゴ
└── ceo.jpg                 # 代表写真（実務感のある写真として代表メッセージで使用）
```

**削除済み**：`app/services/senior-home/`（page.tsx + FacilityRecommender.tsx）。 v4.0 で老人ホーム紹介事業を撤廃。

### 6.4 トップページ（`app/page.tsx`）のセクション構成（v4.0）

`"use client"` コンポーネント。

```
Nav → ① HERO → ② ABOUT → ③ CASES → ④ SERVICE → ⑤ ARCH NOTE → ⑥ MESSAGE → ⑦ CONTACT → Footer
```

- **Nav 項目**：About / Cases / Service / Note + 無料相談（5項目）
- **HERO**：「訪問歯科を、"続けられる事業"へ。」+ 2 CTA（無料相談 / サービスを見る）
- **ABOUT**：「"助言だけ"では、訪問歯科は回らない。」+ 5 特徴グリッド
- **CASES**：3 事例（課題 → 支援内容 → 結果）。医院名は匿名化
- **SERVICE**：5 サービス並列リスト（href は 3 メインページ + 2 アンカー）
- **ARCH NOTE**：厳選 3 本 + `/columns` への「すべて読む」リンク
- **MESSAGE**：代表メッセージ + ceo.jpg
- **CONTACT**：FormSubmit.co 経由のメールフォーム
- **News セクションは v4.0 でトップから撤去**（/news ページと lib/news.ts は引き続き運用）

### 6.5 簡易診断ツール（`dental-matching/DentalMatchingClient.tsx`）

- 6 項目 × 5 段階 Likert、useState で回答管理
- **閾値ロジック**（v4.0 でも保持）：
  - LOW（平均 < 2.5）：即刻変えるべき
  - MID（2.5 ≤ 平均 < 4.0）：現在の歯科医院と相談
  - HIGH（平均 ≥ 4.0）：素晴らしい歯科医院
- Server Component（`page.tsx`）で metadata を export し、Client Component を import する分離構造
- **「ARCHセンサー」「品質診断エンジン」ブランド呼称は撤廃済み**

### 6.6 Git ワークフロー・制約

- **VM（Cowork）からの `git push` は不可**（HTTP プロキシ 403）。VM 側で `git add` + `git commit` まで行い、ユーザー（鈴木氏）に `git push origin main` を依頼する
- ユーザーの push 手順：`cd /Volumes/ARCH_SSD/arch-corporate-site && git push origin main`
- push 後は Vercel が自動デプロイ（通常2〜3分）
- `npm run build`（`next build`）は VM 内では SWC バイナリ欠落により実行不可。`./node_modules/.bin/tsc --noEmit -p tsconfig.json` で型チェックのみ実施。`.next/types/validator.ts` の既存警告（`/columns` vs `/column` ルート不一致）は無視して可
- `.git/index.lock` が macOS Finder 由来で残ることがあるため、git 操作前に `rm -f .git/index.lock` を入れると安全

### 6.7 コラム（ARCH NOTE）記事の追加パターン

1. `app/columns/<slug>/page.tsx` を作成。既存記事（例：`broker-trap`）をテンプレートとして使用
2. `export const metadata: Metadata` で title / description を定義
3. 本文は `<article className="py-16 md:py-24">` → `<div className="max-w-3xl mx-auto px-4 sm:px-6">` → `<div className="prose-custom">` の入れ子構造
4. `app/columns/page.tsx` の `NOTES` 配列の先頭に 1 エントリ追加（slug / category / title / date）
5. `app/columns/layout.tsx` の `ALL_NOTES` 配列にも 1 エントリ追加（関連メモ表示用）
6. 必要に応じ `app/page.tsx` の `NOTES` 配列を更新（トップに掲載する厳選 3 本を入れ替える場合）
7. `lib/news.ts` の `NEWS` 配列先頭にコラム公開エントリを追加（News 追加ワークフロー＝第 5 章に従う）
8. `app/sitemap.ts` に URL 追加

### 6.8 最近のコミット履歴（参考、v4.0 関連）

```
b261f7f feat(notes): ARCH NOTE インデックス新設 + columns レイアウトを editorial v2 化
7ba3266 refactor: dental-matching を補助ツール表現に格下げ＋トップの SERVICES href にアンカー追加
6bae408 refactor(bpo): 事務代行・BPO支援に改題＋13体エージェント表現を完全撤廃＋DX統合
8488fa4 refactor(sales): 施設連携・営業支援に改題＋訪問歯科特化＋品質診断ツール内包
c45b71c refactor(consulting): 歯科医院運営支援に改題＋外部事務長コンセプトで全面書き直し
1f91642 refactor: senior-home 削除 + 関連サービスを新 3 ページ構成に整理
61dacd4 feat: トップページを「訪問歯科の外部事務長」コンセプトに全面改修
```

---

## 7. 変更履歴

- **v4.1 (2026-05-12)**：中核コンセプトを「歯科医院の外部事務長（特に訪問歯科運営に強い）」へ拡張。HERO コピーを「歯科医院を、回り続ける組織へ。」に変更、サブコピーを医院運営全体（外来・訪問歯科・分院展開・スタッフ導線・レセプト・現場オペレーション）に拡張。サービス 01 を「訪問歯科運営支援」→「歯科医院運営支援」にリネーム。CASE 04「分院立ち上げ支援」を新規追加。STRENGTHS の最初を「医院運営に密着」に変更、訪問歯科は別 strength として残す。SEO メタも歯科医院系を主に、訪問歯科を強み枠で配置。
- **v4.0 (2026-05-11)**：**「訪問歯科の外部事務長」一本に集約。** 旧 5 ブランド名（プロデューサー / 営業学部 / 暮らしの選択相談所 / 品質診断エンジン / 外付け事務局）を撤廃し、3 メインページ + 1 補助ツール構成へ。13 体エージェント・AI 前面表現・横文字ブランドの全削除。老人ホーム紹介事業を削除。コラムは ARCH NOTE として整理。ブランド方針節（0 章）を新設。
- **v3.0 (2026-04-20)**：技術アーキテクチャ・サイト構成節（第6章）を追記。
- **v2.1 (2026-04-11)**：News 追加ワークフロー節（第5章）を追記。
- **v2.0 (2026-04-10)**：サービス名全面刷新。5 事業体制に移行（v4.0 で撤廃）。
- **v1.0**：初版リリース。訪問歯科コンサル／営業コンサル／老人ホーム紹介の 3 事業体制。
